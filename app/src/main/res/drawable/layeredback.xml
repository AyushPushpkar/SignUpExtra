<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background Gradient -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:startColor="#4CAF50"
                android:endColor="#2E7D32"
                android:angle="45" />
        </shape>
    </item>

    <!-- Overlay Shape with Rounded Corners -->
    <item android:left="16dp" android:right="16dp" android:top="16dp" android:bottom="16dp">
        <shape android:shape="rectangle">
            <solid android:color="#30FFFFFF" />
            <corners android:radius="16dp" />
        </shape>
    </item>

    <!-- Decorative Element: Circle -->
    <item android:gravity="top|left" android:left="30dp" android:top="30dp">
        <shape android:shape="oval">
            <solid android:color="#80FFFFFF" />
            <size android:width="100dp" android:height="100dp"/>
        </shape>
    </item>

    <!-- Decorative Element: Another Circle -->
    <item android:gravity="bottom|right" android:right="30dp" android:bottom="30dp">
        <shape android:shape="oval">
            <solid android:color="#40FFFFFF" />
            <size android:width="150dp" android:height="150dp"/>
        </shape>
    </item>

</layer-list>
